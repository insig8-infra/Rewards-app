# Forensic Report

**Generated:** 2026-07-01T03:26:49Z  
**Problem:** Contractor management was marked complete after UAT, but the visible `Browse` photo-upload control still appeared unclickable to the user. The user correctly framed this as a process failure: Agentic Engineering was drifting into reactive vibe coding.

---

## Evidence Summary

### Git Activity

- **Last commit:** none. The repository is on `main` with no commits yet.
- **Uncommitted changes:** all project files are currently untracked.
- **Active worktrees:** one worktree.

Because there is no commit history yet, git cannot serve as an audit trail for phase completion, rollback, or repeated-fix detection. Planning artifacts and executable gates must carry more of the control burden until the repo is committed.

### Planning State

- **Current build frame:** `.planning/v1-agentic-build/APPROACH.md` says slices are done only after requirement IDs, server-side rules, tests, UI behavior verification, security/eval checklist, and residual risk documentation.
- **Roadmap expectation:** `.planning/v1-agentic-build/ROADMAP.md` requires vertical slices with tests and review gates.
- **Security/eval expectation:** `.planning/v1-agentic-build/SECURITY_AND_EVALUATION_PLAN.md` evaluates visual/behavioral correctness and trajectory quality, not only build/test output.
- **UI skill expectation:** `.planning/v1-agentic-build/skills/ui_surface_implementation/SKILL.md` existed, but its required checks were too generic and did not require visible-control interaction fidelity.

### Artifact Completeness

| Area | Artifact | Status | Evidence |
|---|---|---:|---|
| Method | `APPROACH.md` | Present | Defines Agentic Engineering posture and Definition of Done. |
| Roadmap | `ROADMAP.md` | Present | Defines milestone exit gates. |
| Security/eval | `SECURITY_AND_EVALUATION_PLAN.md` | Present | Requires visual/behavioral correctness and trajectory quality. |
| Skills | `SKILLS_PLAN.md` | Present | Defines `ui-surface-implementation`, but not enough interaction-fidelity gates. |
| UI skill | `skills/ui_surface_implementation/SKILL.md` | Present | Did not require real file chooser, exact URL parity, screenshot evidence, console/network checks, or persisted readback. |
| Phase 8 plan | `PHASE_8_EXECUTION_PLAN.md` | Present | Claimed browser smoke checks but also notes live browser list showed `Failed to fetch`. |
| Phase 8 status | `PHASE_8_STATUS.md` | Present | Later corrected over-claim and added runtime/browser UAT evidence. |
| Phase 10 status | `PHASE_10_STATUS.md` | Present | Admits initial runtime gate over-claim and corrected actor-contract coverage. |

## Anomalies Detected

### Missing Interaction-Fidelity Gate - Confidence: HIGH

**Evidence:** `PHASE_8_EXECUTION_PLAN.md` says "Contractor photo upload preview rendered from a device file", but the process did not require proving that the visible `Browse` affordance itself opened a native file chooser. The user then reproduced a visible `Browse` click failure.

**Interpretation:** The test path was too implementation-adjacent. A Playwright file upload can prove the hidden input accepts a file, but it does not prove that a real user can activate the visible control.

### Phase Completion Accepted Partial Browser Evidence - Confidence: HIGH

**Evidence:** `PHASE_8_EXECUTION_PLAN.md` simultaneously listed browser smoke checks and noted: "The live browser contractor list showed `Failed to fetch` because the API server was not running..."

**Interpretation:** This should have blocked completion. A UI-bearing phase cannot pass while its live browser path cannot fetch data from the API.

### Runtime Gate Initially Proved The Wrong Boundary - Confidence: HIGH

**Evidence:** `PHASE_10_STATUS.md` says the first passing gate proved API health, Supabase connectivity, and direct API contractor persistence, but not the exact Admin Web browser actor contract.

**Interpretation:** The gate tested a nearby system boundary, not the real browser-to-API boundary used by the product.

### Dev URL Hydration Parity Was Missing - Confidence: HIGH

**Evidence:** The user was testing `http://127.0.0.1:3001`. Next.js dev required `allowedDevOrigins: ["127.0.0.1"]` in `apps/admin-web/next.config.ts`; otherwise the SSR page could load while client-side dev resources and hydration were blocked.

**Interpretation:** Testing only a component, a hidden input, or an alternate host can miss a broken page at the exact URL the user is using.

### Skill/Eval Coverage Was Underspecified - Confidence: MEDIUM

**Evidence:** `ui_surface_implementation/SKILL.md` required rendered workflow tests "where practical" but did not define minimum workflow cases, role-denied checks, visible-control checks, or persisted API/database readback.

**Interpretation:** The skill was aspirational process memory, not an enforceable procedure.

### Git-Based Review Control Is Not Yet Available - Confidence: MEDIUM

**Evidence:** `git log` reports no commits yet and `git status --short` shows all project files as untracked.

**Interpretation:** Until an initial commit exists, the project lacks a reliable diff baseline, commit-level review history, and rollback control. This increases the risk of unnoticed drift.

## Root Cause Hypothesis

The contractor slice failed the Agentic Engineering standard because the phase completion gate accepted indirect evidence: API persistence, component-level checks, and smoke checks were treated as enough. The missing proof was user-observable behavior at the exact deployed local URL, with real controls, real role context, real network calls, and persisted readback after each mutation.

## Recommended Actions

1. Strengthen `ui_surface_implementation` into a mandatory workflow: exact URL, visible-control interaction, browser file chooser where relevant, console/network checks, screenshot evidence, role-denied path, and API/database readback.
2. Update phase plan/completion templates so UI slices cannot be marked complete when the live browser path has failed fetches, hydration errors, or unverified controls.
3. Add a reusable browser UAT script or documented test harness for each UI section before moving to the next feature slice.
4. Treat any user correction after a claimed phase completion as labeled failure data and update the relevant skill/eval artifacts immediately.
5. Create an initial git commit once the current correction is reviewed, so future work has a real audit trail and rollback boundary.

---

*Report generated using the `$gsd-forensics` workflow. Absolute paths and secrets are intentionally omitted.*
